sudo: false
language: cpp
compiler:
  - gcc

before_install:
  - sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
  - sudo apt-get update -q
  - sudo apt-get install gcc-4.9.2 -y

install: true

before_script:
 mkdir build
 cd build
 cmake .. -DCMAKE_BUILD_TYPE=Release
 gcc --version

script:
 make

notifications:
  email:
    recipients:
      - sendspam@mail2nowhere.com
    on_success: always
    on_failure: always
